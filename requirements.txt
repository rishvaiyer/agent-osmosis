# Core engine (runs the full demo on CPU in seconds, no downloads)
numpy>=1.26
scikit-learn>=1.4

# Examples & dashboard
matplotlib>=3.8
plotly>=5.20
streamlit>=1.40

# --- optional: the "real LLM" path (not needed for the demo) ---
# transformers>=4.44
# peft>=0.12
# torch>=2.2        # CPU wheel
# datasets>=2.20
